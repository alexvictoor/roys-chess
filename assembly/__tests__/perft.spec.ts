import { perft } from "../perft";

describe("perft", () => {
  it("should...", () => {
    expect(perft(4)).toBe(197281);
  });
  it("should... 2", () => {
    expect(perft(4)).toBe(197281);
  });
  it("should... 3", () => {
    expect(perft(4)).toBe(197281);
  });
});
